# Dependências (O MAIS IMPORTANTE: Nunca envie isso, é muito pesado)
node_modules
package-lock.json
yarn.lock

# Arquivos de Build (O código final gerado, não o fonte)
dist
build

# Variáveis de Ambiente (SEGURANÇA: Aqui ficam senhas e chaves de API)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs de erro do sistema
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Arquivos de Sistema Operacional (Lixo do Mac/Windows)
.DS_Store
Thumbs.db

# Configurações do Editor (VS Code, etc)
.vscode
.idea
